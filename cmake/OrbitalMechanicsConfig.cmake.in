@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(Threads)

if(@USE_OPENMP@)
    find_dependency(OpenMP)
endif()

if(@USE_HDF5@)
    find_dependency(HDF5 COMPONENTS C CXX)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/OrbitalMechanicsTargets.cmake")

check_required_components(OrbitalMechanics)